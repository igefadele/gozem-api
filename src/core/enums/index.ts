/** 
===================
BASE ENUMS
*/

export enum EntityKey{
  delivery = 'delivery',
  package = 'package',
}

export enum DeliveryStatus {
  open = 'open',
  pickedUp = 'picked-up',
  inTransit = 'in-transit',
  delivered = 'delivered',
  failed = 'failed',
}

